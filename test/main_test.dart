import 'package:flutter_test/flutter_test.dart';

void main() {
  /**
   * Nothing test in here
   *
   * Please run `flutter test` in each module.
   */
  test(
    'Should pass this test. This test created just for passed in codemagic.io.',
    () async {
      // arrange
      final text = 'hello dicoding';

      // assert
      expect(text, 'hello dicoding');
    },
  );
}